# API Manual Test Cases

These cases cover the current API contracts and frontend flows as implemented in the repository.

This table preserves historical observations and records the final regression run
executed on 2026-08-18. Historical failures are marked resolved only when the
same contract was reproduced successfully.

| ID | Endpoint | Objective | Payload | Expected HTTP | Expected result | Actual result | Status | Observations |
|---|---|---|---|---:|---|---|---|---|
| QA-API-001 | `GET /health` | Verify basic backend availability. | N/A | 200 | JSON includes `status: "healthy"`, `service: "Insurance Policy RAG API"`, and `version: "0.5.0"`. | `{"status":"healthy","service":"Insurance Policy RAG API","version":"0.5.0"}` | Passed | Revalidated from the final Docker Compose build on 2026-08-18. |
| QA-API-002 | `GET /config` | Verify the configuration response contract. | N/A | 200 | JSON includes `llm_model`, `embedding_model`, `vector_store`, and `top_k`. | HTTP 200. Active configuration: `gpt-4.1-mini`, `text-embedding-3-small`, local persistent Qdrant, collection `queplan_policies`, `top_k: 5`. | Passed | Revalidated from the final Docker Compose build on 2026-08-18. |
| QA-API-003 | `POST /ask` | Submit a valid minimal question without `policy_id`. | `{"question":"Que cubre la poliza?","mode":"policies"}` | 200 | JSON includes `answer`, `sources`, and `metadata`. | HTTP 200 with a real non-mock answer, five policy sources and `metadata.latency_ms`. | Passed | Revalidated through the same final policies-mode E2E used for release certification. |
| QA-API-004 | `POST /ask` | Submit a valid question with `policy_id`. | `{"question":"¿Qué coberturas hospitalarias contempla la póliza?","policy_id":"POL320190074"}` | 200 | If dependencies respond successfully, JSON includes `answer`, `sources`, and `metadata`; retrieval applies a `policy_id` filter if it reaches Qdrant. | HTTP 200 OK. Real execution, not mock. Metadata: `model: "gpt-4.1-mini"`, `embedding_model: "text-embedding-3-small"`, `policy_id: "POL320190074"`, `retrieved_chunks: 5`, `response_time_ms: 9185.69`, `is_mock: false`. Sources: `POL320190074.pdf - Página 20 - ARTÍCULO Nº 2`; `POL320190074.pdf - Página 19 - ARTÍCULO Nº 2`; `POL320190074.pdf - Página 1 - ARTÍCULO 2º`; `POL320190074.pdf - Página 35 - ARTÍCULO N° 3`; `POL320190074.pdf - Página 18 - ARTÍCULO Nº 2`. Retrieval scores: `0.7047`, `0.6797`, `0.6747`, `0.6671`, `0.6669`. The answer described hospital coverage such as bed days, hospital services, surgical medical fees, and dental surgery due to accident, and included textual references `[Fuente 1]`, `[Fuente 2]`, and `[Fuente 3]`. Manual PDF verification completed: page 20 directly supports bed days, hospital services, surgical medical fees, and dental surgery due to accident; page 19 supports explicit benefits in the particular conditions, percentages, reimbursement or payment limits, applicable conditions, and the general hospitalization benefit; page 1 contains Article 2, supports the start of the hospitalization benefit, bed days, and hospital services; page 18 introduces the list of grantable coverages and includes hospitalization benefit; page 35 adds context through hospital and hospitalization definitions, does not directly support all listed coverages, and does not contradict the answer. | Passed | Manual validation against `POL320190074.pdf` was completed. The main answer claims are supported by the cited pages, no contradictions were detected, and no evident hallucinations were detected in this case. Page 35 was retrieved as additional context. This question can remain a demo candidate. |
| QA-API-005 | `POST /ask` | Validate missing required `question` field. | `{"policy_id":"POL320190074"}` | 422 | FastAPI/Pydantic validation error because `question` is required. | HTTP 422. Validation: `body.question`, field required. | Passed | Executed manually from PowerShell with `Invoke-RestMethod` against Docker Compose API at `http://localhost:8000`. |
| QA-API-006 | `POST /ask` | Validate empty JSON object. | `{}` | 422 | FastAPI/Pydantic validation error because `question` is required. | HTTP 422. Validation: `body.question`, field required. | Passed | Executed manually from PowerShell with `Invoke-RestMethod` against Docker Compose API at `http://localhost:8000`. |
| QA-API-007 | `POST /ask` | Validate empty string question. | `{"question":""}` | 422 | FastAPI/Pydantic validation error because `question` has `min_length=3`. | HTTP 422. Validation: `string_too_short`, minimum length 3. | Passed | Schema-level validation. |
| QA-API-008 | `POST /ask` | Validate question shorter than three characters. | `{"question":"ab"}` | 422 | FastAPI/Pydantic validation error because `question` has `min_length=3`. | HTTP 422. Validation: `string_too_short`, minimum length 3. | Passed | Schema-level validation. |
| QA-API-009 | `POST /ask` | Validate whitespace-only question that passes length validation but fails service validation. | `{"question":"   "}` | 400 | `RealRAGService` strips the question, raises `ValueError`, and the API returns `detail` starting with `Parámetros de consulta no válidos:`. | HTTP 400. Detail: `Parámetros de consulta no válidos: question must not be empty`. | Passed | Service-level validation after schema parsing; this is intentionally distinct from HTTP 422 schema validation. |
| QA-API-010 | `POST /ask` | Validate wrong type for `question`. | `{"question":123}` | 422 | FastAPI/Pydantic validation error because `question` is declared as `str`. | HTTP 422. Validation: `string_type` for `body.question`. | Passed | Schema-level validation. |
| QA-API-011 | `POST /ask` | Validate `question` as null. | `{"question":null}` | 422 | FastAPI/Pydantic validation error because `question` is required and not nullable. | HTTP 422. Validation: `string_type` for `body.question`. | Passed | Schema-level validation. |
| QA-API-012 | `POST /ask` | Validate wrong type for `policy_id`. | `{"question":"What does the policy cover?","policy_id":123}` | 422 | FastAPI/Pydantic validation error because `policy_id` is declared as `str \| None`. | HTTP 422. Validation: `string_type` for `body.policy_id`. | Passed | Schema-level validation. |
| QA-API-013 | `POST /ask` | Validate explicit null `policy_id`. | `{"question":"What does the policy cover?","policy_id":null}` | 200 | Schema accepts `policy_id: null`; runtime proceeds without a policy filter. | Request accepted and a normal answer returned. | Passed | This is not an invalid request. The schema allows `policy_id` to be null. The question was written in English. Any visible character-encoding issue in the PowerShell-rendered answer is treated as a console-display issue only, not an API validation failure. |
| QA-API-014 | `POST /ask` | Validate extra undefined field behavior. | `{"question":"What does the policy cover?","unexpected":"value"}` | 200 | Current schema does not explicitly forbid extra fields; request is accepted and the unknown field is ignored. | Request accepted and a normal answer returned. | Passed | This is not rejected by the current schema. Extra fields are ignored. The question was written in English. |
| QA-API-015 | `POST /ask` | Validate array body instead of object. | `[]` | 422 | FastAPI/Pydantic validation error because the endpoint expects an object compatible with `AskRequest`. | HTTP 422. Validation: `model_attributes_type`; body must be a valid dictionary or object. | Passed | Schema-level body-shape validation. |
| QA-API-016 | `POST /ask` | Validate string body instead of object. | `"What does the policy cover?"` | 422 | FastAPI/Pydantic validation error because the endpoint expects an object compatible with `AskRequest`. | HTTP 422. Validation: `model_attributes_type`; body must be a valid dictionary or object. | Passed | Schema-level body-shape validation. |
| QA-API-017 | `GET /ready` | Verify readiness before creating the local index. | N/A | 200 | JSON reports `status: "not_ready"`, `openai_configured: true`, `index_ready: false`, collection `queplan_policies`, `indexed_chunks: 0`, and a diagnostic detail explaining that the collection does not exist. | `{"status":"not_ready","openai_configured":true,"index_ready":false,"collection":"queplan_policies","indexed_chunks":0,"detail":"Qdrant collection 'queplan_policies' does not exist; run the indexing command first"}` | Passed | Confirmed that OpenAI was detected as configured, the collection did not exist yet, readiness was `not_ready`, and the diagnostic message was clear. |
| QA-API-018 | `GET /ready` | Verify readiness after creating the local index. | N/A | 200 | JSON reports `status: "ready"`, `openai_configured: true`, `index_ready: true`, collection `queplan_policies`, `indexed_chunks: 262`, and `detail: null`. | `{"status":"ready","openai_configured":true,"index_ready":true,"collection":"queplan_policies","indexed_chunks":262,"detail":null}` | Passed | Revalidated on 2026-08-18 against the final nine-PDF index. |
| QA-API-019 | `POST /ask` | Retest PowerShell execution after a non-parseable request body. | PowerShell `Invoke-RestMethod` using `$body = @{...} | ConvertTo-Json`. | 200 | The endpoint accepts valid JSON equivalent to QA-API-004. | HTTP 200 with `answer`, `sources` and `metadata`. | Passed | The historical failure was caused by malformed local request construction, not by `/ask`. The final PowerShell request and Docker E2E both passed. |
| QA-API-020 | `POST /ask` | Validate malformed JSON request body. | `{"question":"What does the policy cover?"` | 422 | FastAPI request parsing rejects the body before endpoint logic because the request body is not valid JSON. | HTTP 422 Unprocessable Entity. Validation type: `json_invalid`. Message: JSON decode error. | Passed | Schema/request parsing validation. The request was rejected before reaching endpoint logic. |
| QA-API-021 | `POST /ask` | Validate invalid `Content-Type`. | Content-Type: `text/plain`; body: `{"question":"What does the policy cover?"}` | 422 | FastAPI interprets the request body as plain text instead of a JSON object, then schema validation rejects it. | HTTP 422 Unprocessable Entity. Validation type: `model_attributes_type`. Message: `Input should be a valid dictionary or object to extract fields from`. | Passed | Schema-level validation before endpoint execution. This is distinct from service-level HTTP 400 validation and runtime application errors. |

## Direct API Draft Test Cases

| ID | Endpoint | Objective | Payload | Expected HTTP | Expected result | Actual result | Status | Observations |
|---|---|---|---|---:|---|---|---|---|
| API-DRAFT-001 | `POST /generate-policy` | Validate successful policy draft generation directly from Swagger. | `{"instructions":"Create a new health insurance policy for adults. Keep hospitalization, emergency care and surgery coverage, strengthen catastrophic coverage, include outpatient consultations and diagnostic tests, and exclude pre-existing conditions and cosmetic procedures.","source_policy_ids":["POL320190074"]}` | 200 | Draft response includes `draft`, `sources`, `metadata`, and `disclaimer`. | HTTP 200 OK. Draft generated correctly. Sources present. Metadata present. Disclaimer present. Metadata included `model: "gpt-4.1-mini"`, `retrieved_chunks: 3`, and `is_mock: false`. No errors occurred. | Passed | Successful direct API draft generation verified from Swagger. |
| API-DRAFT-002 | `POST /generate-policy` | Validate minimum length for `instructions`. | `{"instructions":"Hi","source_policy_ids":["POL320190074"]}` | 422 | Validation error for `instructions` minimum length. | HTTP 422. Field: `instructions`. Validation: minimum length = 10. | Passed | Input validation rejected the request before draft generation. |
| API-DRAFT-003 | `POST /generate-policy` | Robustness test for malformed JSON. | Malformed JSON with an additional unmatched brace. | 422 | JSON decode error before endpoint logic is executed. | HTTP 422. JSON decode error. The parser correctly rejected malformed JSON before reaching endpoint logic. | Passed | Robustness test passed. |
| API-DRAFT-004 | `POST /generate-policy` | Validate minimum number of source policy IDs. | `{"instructions":"Create a health insurance policy for adults.","source_policy_ids":[]}` | 422 | Validation error for `source_policy_ids` minimum items. | HTTP 422. Field: `source_policy_ids`. Validation: minimum items = 1. Observed message was equivalent to `List should have at least 1 item after validation.` | Passed | Input validation rejected an empty source policy list before draft generation. |

## Frontend Manual Test Cases

| ID | Endpoint | Objective | Payload | Expected HTTP | Expected result | Actual result | Status | Observations |
|---|---|---|---|---:|---|---|---|---|
| FE-POL-001 | Chainlit frontend -> `POST /ask` | First real frontend end-to-end smoke test: Chainlit frontend -> FastAPI -> Qdrant -> OpenAI -> Chainlit frontend in `policies` mode with a `policy_id` filter. | Preconditions: `/config` checked from Chainlit. Commands: `/mode policies`; `/policy POL320190074`. Question: `What hospitalization expenses are covered by this policy?` | 200 through backend call | Frontend connects to API, accepts `/mode policies`, accepts `/policy POL320190074`, sends the query to the backend, displays a complete real response, shows route `policies`, renders sources in the UI, finishes without infinite loading, and does not return a mock response. | Chainlit `/config` displayed: `LLM model: gpt-4.1-mini`; `Router model: gpt-4.1-mini`; `Web model: gpt-5.6-luna`; `Embedding model: text-embedding-3-small`; `Vector store: Qdrant (local persistent)`; `Collection: queplan_policies`; `Top k: 5`; `Score threshold: None`; `Available modes: auto, policies, web, combined`; `OpenAI configured: True`. Commands `/mode policies` and `/policy POL320190074` were accepted. The response rendered completely in Chainlit, route was `policies`, sources were visible, no errors occurred, no infinite loading occurred, and the response was not mock. Sources shown: `Fuente 1: POL320190074.pdf - Página 16 - ARTÍCULO Nº 2`; `Fuente 2: POL320190074.pdf - Página 20 - ARTÍCULO Nº 2`; `Fuente 3: POL320190074.pdf - Página 1 - ARTÍCULO 2º`; `Fuente 4: POL320190074.pdf - Página 3 - ARTÍCULO 3º`; `Fuente 5: POL320190074.pdf - Página 4 - ARTÍCULO 4º`. Manual grounding: page 16 supports the start of coverage and that coverage must be contracted and indicated in the Particular Conditions; page 20 supports Días Cama Hospitalización, Servicios Hospitalarios, Honorarios Médicos Quirúrgicos, and Cirugía Dental por Accidente; page 1 contains Article 2 and supports Hospitalization Benefit, Días Cama, and Servicios Hospitalarios; page 3 provides definitions of accident, illness, disability, reasonable medical expenses, and expenses actually incurred; page 4 is Article 4 exclusions, adds context, does not directly support the main coverage list, and does not contradict the response. | Passed | First frontend end-to-end smoke test validated. Main claims are supported by cited pages, no contradictions were detected, no evident hallucinations were detected, page 4 was additional context, and sources were visible and traceable from the frontend. Language observation only: the question was submitted in English, while the assistant answered in Spanish, matching the source policy language. The expected UX language behavior should be confirmed with the team. |
| FE-POL-NOEVIDENCE-001 | Chainlit frontend -> `/mode policies` -> `POST /ask` | Validate policies-mode behavior when a policy-specific question asks about a scenario not explicitly covered by the policy. | Mode: `policies`. Policy: `POL320190074`. Question: `Does this policy cover damages caused by a spacecraft collision?` | 200 through backend call | Assistant should not fabricate policy content. It should acknowledge that the policy does not explicitly address the scenario, explain that there is insufficient evidence to conclude whether such coverage exists, and cite retrieved policy sources. | The assistant explicitly stated that the policy does not mention spacecraft collisions, explained that the available evidence was insufficient to determine whether coverage exists, and supported the response with retrieved policy sections. | Passed | Functional no-evidence behavior validated in policies mode. This is not a schema validation case. |
| FE-OOS-001 | Chainlit frontend -> `/mode policies` -> `POST /ask` | Validate out-of-scope creative request handling while operating in policies mode. | Mode: `policies`. Question: `Write a short poem about the ocean.` | 200 through backend call | Assistant should not generate unsupported creative content. It should explain that the request is outside the available policy evidence and avoid hallucinating. | The assistant stated that no literary information about the ocean was available in the retrieved policy corpus, declined to generate the requested poem based on unsupported evidence, and cited the retrieved policy sources. | Passed | Functional out-of-scope behavior validated in policies mode. This is not an API/schema validation case. |
| FE-WEB-001 | Chainlit frontend -> `/mode web` -> `POST /ask` | Regression-test the explicit web route after the empty-output fix. | Preconditions: `/ready` and `/config` return 200. Command: `/mode web`. Original question: `What are the most relevant recent developments in Ecuador's insurance sector?` | 200 through backend call | Frontend displays a web-route answer or a controlled degraded response, never an internal HTTP 500. | The original query returned HTTP 200 with web sources after the fix. A final Chile insurance-regulation query also returned HTTP 200 with 12 web sources and visible latency. | Passed | Resolved by using low reasoning effort, a larger output budget and controlled handling of empty model output. Final evidence: `13-web-final.png`. |
| FE-COMB-001 | Chainlit frontend -> `/mode combined` -> `POST /ask` | Validate combined mode with policy evidence and current web information. | Command: `/mode combined`. Question: `Compare the catastrophic coverage described in the policy with recent developments in Ecuador's insurance sector.` | 200 through backend call | Frontend displays route `combined`, includes separate `Evidencia de pólizas` and `Información web actual` blocks, displays PDF sources, and displays web sources with URLs. | The query responded correctly. Route displayed: `combined`. The response included two blocks: `Evidencia de pólizas` and `Información web actual`. PDF sources were displayed. Web sources with URLs were displayed. Sources from `POL320190074.pdf` were retrieved. Web sources included domains such as `supercias.gob.ec` and `undp.org`. No HTTP 500 occurred. | Passed | Policy sources: Displayed. Web sources: Displayed. UX language observation: the combined response contained mixed languages; the policy section was mostly in Spanish, while part of the web section was in English. This is a language consistency observation, not a blocking bug. |
| FE-WEB-002 | Chainlit frontend -> `/mode web` -> `POST /ask` | Validate explicit web mode with a general web query. | Command: `/mode web`. Question: `What is artificial intelligence?` | 200 through backend call | Frontend displays a coherent web-route response without internal errors. | The query responded correctly. The response was shown in Chainlit. Route displayed: `web`. No HTTP 500 occurred. The response was coherent and did not show internal errors. | Passed | General web queries can execute successfully. This narrows `FE-WEB-001` to a query-specific or result-specific failure rather than a complete failure of web mode. |
| FE-AUTO-001 | Chainlit frontend -> `/mode auto` -> `POST /ask` | Validate automatic routing for a general knowledge question. | Mode: `auto`. Question: `Who won the FIFA World Cup in 2022?` | 200 through backend call | Router should classify the question as general knowledge, use the web route, and answer without querying the policy corpus. | The assistant correctly answered that Argentina won the 2022 FIFA World Cup and displayed route `web`. | Passed | Routing behavior validated. This is a functional routing case, not an API/schema validation case. |
| FE-ERR-API-OFF-001 | Chainlit frontend with FastAPI stopped | Validate frontend behavior when the backend API is unavailable in Docker Compose. | Procedure: start full stack with `docker compose up -d`; stop only API with `docker compose stop api`; verify frontend remained healthy; open Chainlit; run `/mode policies`; run `/policy POL320190074`; ask `What hospital coverage does this policy provide?`; restart API with `docker compose start api`. | N/A | Frontend remains responsive, shows a controlled backend-unavailable message, avoids exposing traceback or internal exception details, and does not remain in infinite loading. | The frontend remained responsive. Chainlit displayed: `The backend is unreachable right now. Confirm the API is running and the configured URL is correct.` No traceback or internal exception was exposed to the user. No infinite loading occurred. Chainlit continued running normally. Docker logs only contained translation/logo warnings unrelated to the API failure. API was restarted with `docker compose start api`. | Passed | API-off frontend validation completed successfully in Docker Compose. |
| FE-TIMEOUT-001 | Chainlit frontend with backend response timeout | Validate frontend behavior when the backend exceeds the response timeout. | Procedure: pause the API container and submit a policy query from the frontend. | N/A | Frontend should detect the timeout, display a user-friendly error message, remain responsive, and avoid crashing or loading indefinitely. | Chainlit displayed: `The backend took too long to respond. Please try again.` The application remained responsive. No crash, traceback, or infinite loading occurred. | Passed | Frontend timeout handling validated. This is a functional frontend resilience case, not an API/schema validation case. |
| FE-DRAFT-001 | Chainlit frontend command validation | Validate invalid `/draft` command usage. | Command: `/draft` | N/A | Frontend shows the expected draft syntax instead of failing. | The system responded: `Use /draft POL1,POL2 \| instructions with one to three policy ids and at least 10 characters of instructions.` | Passed | Invalid command usage is handled gracefully by showing the expected syntax. |
| FE-DRAFT-002 | Chainlit frontend -> `/draft` -> `POST /generate-policy` | Validate successful draft generation from a policy source. | Command: `/draft POL320190074 \| Create a new health insurance policy for adults. Keep hospitalization, emergency care and surgery coverage, strengthen catastrophic coverage, include outpatient consultations and diagnostic tests, and exclude pre-existing conditions and cosmetic procedures.` | 200 through backend call | Draft is generated, policy evidence is used, PDF sources are cited, disclaimer is included, and missing information is left for human definition instead of invented. | The draft was generated correctly. No backend errors occurred. No HTTP 500 occurred. Policy information was used. PDF sources were cited. The document was structured by sections. It included a warning that legal and actuarial review is required. When specific information was missing, such as limits, deductibles, and catastrophic coverage, the system marked it for manual definition instead of inventing it. Sources used: `POL320190074.pdf`, page 16, page 2, and page 6. | Passed | UX improvement: references such as `[Fuente 3, u.]` and `[Fuente 3, q.vi]` may be unclear for end users. This is not a functional bug. Positive responsible-AI behavior observed: `Coverage for catastrophic events (to be defined...)` remained deliberately open and the system did not invent missing information. |
