# API Manual Test Cases

These cases cover the current contracts for `/health`, `/config`, and `/ask` as implemented in the repository.

Leave "Actual result" empty until each case is executed manually from Swagger. Keep "Status" as `Not executed` until the case is actually run.

| ID | Endpoint | Objective | Payload | Expected HTTP | Expected result | Actual result | Status | Observations |
|---|---|---|---|---:|---|---|---|---|
| QA-API-001 | `GET /health` | Verify basic backend availability. | N/A | 200 | JSON includes `status: "healthy"`, `service: "Insurance Policy RAG API"`, and `version: "0.1.0"`. Actual HTTP: 200. | `{"status":"healthy","service":"Insurance Policy RAG API","version":"0.1.0"}` | Passed | Response matches the expected contract. |
| QA-API-002 | `GET /config` | Verify the configuration response contract. | N/A | 200 | JSON includes `llm_model`, `embedding_model`, `vector_store`, and `top_k`. `llm_model` uses `OPENAI_CHAT_MODEL` or `gpt-4.1-mini`; `vector_store` uses `VECTOR_STORE_TYPE` or `Chroma`. |  | Not executed | Values may vary with environment variables. |
| QA-API-003 | `POST /ask` | Submit a valid minimal question without `policy_id`. | `{"question":"Que cubre la poliza?"}` | To confirm | If dependencies respond successfully, JSON includes `answer`, `sources`, and `metadata`. Otherwise the result depends on OpenAI, Qdrant, local index, and environment variables. |  | Not executed | Do not mark as blocked until executed and dependency blockage is confirmed. |
| QA-API-004 | `POST /ask` | Submit a valid question with `policy_id`. | `{"question":"¿Qué coberturas hospitalarias contempla la póliza?","policy_id":"POL320190074"}` | 200 | If dependencies respond successfully, JSON includes `answer`, `sources`, and `metadata`; retrieval applies a `policy_id` filter if it reaches Qdrant. | HTTP 200 OK. Real execution, not mock. Metadata: `model: "gpt-4.1-mini"`, `embedding_model: "text-embedding-3-small"`, `policy_id: "POL320190074"`, `retrieved_chunks: 5`, `response_time_ms: 9185.69`, `is_mock: false`. Sources: `POL320190074.pdf - Página 20 - ARTÍCULO Nº 2`; `POL320190074.pdf - Página 19 - ARTÍCULO Nº 2`; `POL320190074.pdf - Página 1 - ARTÍCULO 2º`; `POL320190074.pdf - Página 35 - ARTÍCULO N° 3`; `POL320190074.pdf - Página 18 - ARTÍCULO Nº 2`. Retrieval scores: `0.7047`, `0.6797`, `0.6747`, `0.6671`, `0.6669`. The answer described hospital coverage such as bed days, hospital services, surgical medical fees, and dental surgery due to accident, and included textual references `[Fuente 1]`, `[Fuente 2]`, and `[Fuente 3]`. Manual PDF verification completed: page 20 directly supports bed days, hospital services, surgical medical fees, and dental surgery due to accident; page 19 supports explicit benefits in the particular conditions, percentages, reimbursement or payment limits, applicable conditions, and the general hospitalization benefit; page 1 contains Article 2, supports the start of the hospitalization benefit, bed days, and hospital services; page 18 introduces the list of grantable coverages and includes hospitalization benefit; page 35 adds context through hospital and hospitalization definitions, does not directly support all listed coverages, and does not contradict the answer. | Passed | Manual validation against `POL320190074.pdf` was completed. The main answer claims are supported by the cited pages, no contradictions were detected, and no evident hallucinations were detected in this case. Page 35 was retrieved as additional context. This question can remain a demo candidate. |
| QA-API-005 | `POST /ask` | Validate missing required `question` field. | `{"policy_id":"POL320190074"}` | 422 | FastAPI/Pydantic validation error because `question` is required. |  | Not executed | Deterministic according to schema. |
| QA-API-006 | `POST /ask` | Validate empty JSON object. | `{}` | 422 | FastAPI/Pydantic validation error because `question` is required. |  | Not executed | Deterministic according to schema. |
| QA-API-007 | `POST /ask` | Validate empty string question. | `{"question":""}` | 422 | FastAPI/Pydantic validation error because `question` has `min_length=3`. |  | Not executed | Deterministic according to schema. |
| QA-API-008 | `POST /ask` | Validate question shorter than three characters. | `{"question":"ab"}` | 422 | FastAPI/Pydantic validation error because `question` has `min_length=3`. |  | Not executed | Deterministic according to schema. |
| QA-API-009 | `POST /ask` | Validate whitespace-only question that passes length validation but fails service validation. | `{"question":"   "}` | 400 | `RealRAGService` strips the question, raises `ValueError`, and the API returns `detail` starting with `Parametros de consulta no validos:`. |  | Not executed | Expected detail includes `question must not be empty`. |
| QA-API-010 | `POST /ask` | Validate wrong type for `question`. | `{"question":123}` | 422 | FastAPI/Pydantic validation error because `question` is declared as `str`. |  | Not executed | Deterministic according to schema. |
| QA-API-011 | `POST /ask` | Validate `question` as null. | `{"question":null}` | 422 | FastAPI/Pydantic validation error because `question` is required and not nullable. |  | Not executed | Deterministic according to schema. |
| QA-API-012 | `POST /ask` | Validate wrong type for `policy_id`. | `{"question":"Que cubre la poliza?","policy_id":123}` | 422 | FastAPI/Pydantic validation error because `policy_id` is declared as `str \| None`. |  | Not executed | Deterministic according to schema. |
| QA-API-013 | `POST /ask` | Validate explicit null `policy_id`. | `{"question":"Que cubre la poliza?","policy_id":null}` | To confirm | Schema accepts `policy_id: null`; runtime result depends on OpenAI, Qdrant, local index, and environment variables. |  | Not executed | Do not mark as blocked until executed and dependency blockage is confirmed. |
| QA-API-014 | `POST /ask` | Validate extra undefined field behavior. | `{"question":"Que cubre la poliza?","unexpected":"value"}` | To confirm | Current schema does not explicitly forbid extra fields. Runtime result depends on request parsing and, if accepted, OpenAI, Qdrant, local index, and environment variables. |  | Not executed | Confirm actual behavior from Swagger before recording a result. |
| QA-API-015 | `POST /ask` | Validate array body instead of object. | `[]` | 422 | FastAPI/Pydantic validation error because the endpoint expects an object compatible with `AskRequest`. |  | Not executed | Deterministic according to endpoint request model. |
| QA-API-016 | `POST /ask` | Validate string body instead of object. | `"Que cubre la poliza?"` | 422 | FastAPI/Pydantic validation error because the endpoint expects an object compatible with `AskRequest`. |  | Not executed | Deterministic according to endpoint request model. |
| QA-API-017 | `GET /ready` | Verify readiness before creating the local index. | N/A | 200 | JSON reports `status: "not_ready"`, `openai_configured: true`, `index_ready: false`, collection `queplan_policies`, `indexed_chunks: 0`, and a diagnostic detail explaining that the collection does not exist. | `{"status":"not_ready","openai_configured":true,"index_ready":false,"collection":"queplan_policies","indexed_chunks":0,"detail":"Qdrant collection 'queplan_policies' does not exist; run the indexing command first"}` | Passed | Confirmed that OpenAI was detected as configured, the collection did not exist yet, readiness was `not_ready`, and the diagnostic message was clear. |
| QA-API-018 | `GET /ready` | Verify readiness after creating the local index. | N/A | 200 | JSON reports `status: "ready"`, `openai_configured: true`, `index_ready: true`, collection `queplan_policies`, `indexed_chunks: 487`, and `detail: null`. | `{"status":"ready","openai_configured":true,"index_ready":true,"collection":"queplan_policies","indexed_chunks":487,"detail":null}` | Passed | Confirmed that OpenAI was detected as configured, the index was available, the expected collection was detected, 487 chunks were reported, and readiness changed from `not_ready` to `ready`. |
| QA-API-019 | `POST /ask` | Retest PowerShell execution after a non-parseable request body. | PowerShell `Invoke-RestMethod` using local `$body` variable. | To confirm | The endpoint should accept valid JSON equivalent to QA-API-004. | `Invoke-RestMethod : {"detail":"There was an error parsing the body"}` | Needs retest | Do not classify as a confirmed `/ask` product bug because the same endpoint returned HTTP 200 from Swagger. The likely area to retest is preparation or content of the local `$body` variable, but no definitive cause is recorded without reproduction. |
